app.get("/kavya",(req,res)=>{


})

// This is a simple Express.js route handler for the GET request to the "/kavya" endpoint. When a client makes a GET request to this endpoint, the callback function will be executed. However, currently, the callback function is empty and does not send any response back to the client.